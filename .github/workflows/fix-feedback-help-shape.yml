name: Retired mastery portal patch helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mastery readiness and parent decision UI are deployed; this helper is inactive."
