name: Retired mastery decision patch helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mastery readiness, parent decision stamps, and decision-only review handling are deployed; this helper is inactive."
