name: Retired student page controls helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Shared lesson-page navigation controls are installed; this temporary helper is inactive."
