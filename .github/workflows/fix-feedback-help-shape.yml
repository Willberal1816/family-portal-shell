name: Retired teaching-work separation helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Teaching/work separation and worksheet standards are now part of the homeschool authority; this one-time helper is inactive."
