name: Retired iOS paper-link helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "iOS paper and mastery-check links now open the source PDF directly; this helper is inactive."
