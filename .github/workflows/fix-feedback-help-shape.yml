name: Retired student page controls helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Shared Back to School Portal controls are installed across student-facing homeschool pages; this helper is inactive."
