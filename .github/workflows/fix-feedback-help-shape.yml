name: Retired mastery step-order helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mastery lesson step order is deployed globally; this helper is inactive."
