name: Retired mastery label helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mastery lesson ordering and mastery-check labels are deployed globally; this helper is inactive."
