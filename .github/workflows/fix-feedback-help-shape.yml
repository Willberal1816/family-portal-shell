name: Retired complete student class workflow helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The complete student class lifecycle repair is installed. Ongoing enforcement is handled by validate-student-class-flow.yml."
