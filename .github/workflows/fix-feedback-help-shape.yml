name: Retired student feedback contract repair helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Student feedback contract repair is deployed; this helper is inactive."
