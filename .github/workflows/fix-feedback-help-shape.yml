name: Retired portal class continuity helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Active-class continuity and same-session lesson navigation are installed; this helper is inactive."
