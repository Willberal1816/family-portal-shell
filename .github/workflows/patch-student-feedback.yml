name: Retired student feedback patch helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Student lesson feedback repair is already deployed. This helper is intentionally inactive."
